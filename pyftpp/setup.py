from setuptools import setup

setup(
    name='PyFTPP',
    version='0.1.0',
    description='Code to run the FTPP standalone executable',
    author='Renato Bellotti',
    packages=['pyftpp'],
    install_requires=[
        'numpy',
    ],
)
